# rich-gradient-cli

Lets you print gradients from the terminal.

## Installation

You can install `rich-gradient-cli` using uv or pip:

```shell
uv pip install rich-gradient-cli
```

```bash
pip install rich-gradient-cli
```
